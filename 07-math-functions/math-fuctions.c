#include <stdio.h>
#include <math.h>

int main()
{
    float x = 45;

    // x = sqrt(x); Square Root
    // x = pow(x, 4); power  
    // x = round(x); Rounds to cloested number
    // x = ceil(x) Always rounds up 
    // x =  floor(x); Always roll down
    // x = abs(x); Absolute number
    // x = log(x); Log of the number
    // x = sin(x); //sine of the number
    // x = cos(x); // cosine of the number
    // x = tan(x); //tangent of number

    printf("%f", x);

    return 0;
}