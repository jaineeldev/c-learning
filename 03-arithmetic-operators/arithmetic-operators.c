#include <stdio.h>

int main() {

    // arithmetic operators = + - * / % ++ --
    // If divisiing by an int, wont retain the decimal numbers
    int x = 10;
    int y = 2;
    int z = 0;

    // + 
    // z = x + y;

    // -
    // z = x - y;

    // Multiplication
    // z = x * y;

    // /(Division)
    // z = x / y;

    // % (Modulas, gives the remainer of any devision, like how many is left ove
    // when you split 10 students in  groups of 2, none left over so it will show 0)
    // z = x % y;

    // ++ (Incirement)

    // x++; //Adds  1

    // -- (Decirment)

    x--;

    printf("%d",x);

    return 0;
}